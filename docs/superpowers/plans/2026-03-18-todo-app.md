# TODO App Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** React 学習用の最小 TODO アプリを、`localStorage` 永続化と軽い責務分離つきで実装する

**Architecture:** 空のリポジトリに Vite + React + TypeScript + Vitest の最小構成を手動で作る。`App` はレイアウト、`useTodos` は状態管理と永続化、`TodoForm` / `TodoList` / `TodoItem` は表示責務に限定する。テスト基盤のセットアップ後は、フックと UI の振る舞いを小さくテストしながら組み立てる。

**Tech Stack:** React, TypeScript, Vite, Vitest, React Testing Library, jsdom, localStorage

---

## File Structure

- Create: `.gitignore`
  - `node_modules`、`dist`、`coverage`、`.superpowers` を除外する
- Create: `package.json`
  - 開発・ビルド・テストの scripts と依存関係を定義する
- Create: `tsconfig.json`
  - TypeScript の共通設定を持つ
- Create: `vite.config.ts`
  - React プラグインと Vitest 設定を持つ
- Create: `index.html`
  - Vite のエントリ HTML
- Create: `src/main.tsx`
  - React アプリの起点
- Create: `src/index.css`
  - 背景やフォント、ページ全体の基礎スタイル
- Create: `src/App.tsx`
  - 画面全体の構成とコンポーネント結合
- Create: `src/App.css`
  - Calm Panel ベースのレイアウトと見た目
- Create: `src/types/todo.ts`
  - `Todo` 型定義
- Create: `src/hooks/useTodos.ts`
  - TODO 状態、追加、完了切替、削除、`localStorage` 永続化
- Create: `src/components/TodoForm.tsx`
  - 入力欄と追加ボタン
- Create: `src/components/TodoList.tsx`
  - 一覧表示と空状態
- Create: `src/components/TodoItem.tsx`
  - 1 件表示、完了切替、削除ボタン
- Create: `src/test/setup.ts`
  - `jest-dom` 設定などテスト共通初期化
- Create: `src/hooks/useTodos.test.tsx`
  - フックのユニットテスト
- Create: `src/components/TodoForm.test.tsx`
  - 入力と送信のテスト
- Create: `src/components/TodoList.test.tsx`
  - 空状態、一覧、イベント委譲のテスト
- Create: `src/App.test.tsx`
  - 画面全体の統合テスト

### Task 1: 開発基盤を作る

**Files:**
- Create: `.gitignore`
- Create: `package.json`
- Create: `tsconfig.json`
- Create: `vite.config.ts`
- Create: `index.html`
- Create: `src/main.tsx`
- Create: `src/index.css`
- Create: `src/test/setup.ts`

- [ ] **Step 1: プロジェクト設定ファイルを書く**

```gitignore
node_modules
dist
coverage
.superpowers
```

```json
{
  "name": "react-test-2026-03-28",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest",
    "test:run": "vitest run"
  }
}
```

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["DOM", "DOM.Iterable", "ES2022"],
    "allowJs": false,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "module": "ESNext",
    "moduleResolution": "Node",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx"
  },
  "include": ["src"],
  "references": []
}
```

```ts
// vite.config.ts
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  test: {
    environment: "jsdom",
    setupFiles: "./src/test/setup.ts",
    globals: true
  }
});
```

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Todo App</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 2: 依存関係をインストールする**

Run:

```bash
npm install react react-dom
npm install -D typescript vite @vitejs/plugin-react vitest jsdom @testing-library/react @testing-library/jest-dom @testing-library/user-event @types/react @types/react-dom
```

Expected: 依存関係追加の完了メッセージが表示され、エラー終了しない

- [ ] **Step 3: エントリポイントとテストセットアップを最小構成で置く**

```tsx
// src/main.tsx
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

```ts
// src/test/setup.ts
import "@testing-library/jest-dom";
```

- [ ] **Step 4: ビルド前提が成立することを確認する**

Run:

```bash
npm run test:run
```

Expected: テストファイル未作成のため `No test files found` か、0 件実行のメッセージが出る

### Task 2: `useTodos` フックを TDD で作る

**Files:**
- Create: `src/types/todo.ts`
- Create: `src/hooks/useTodos.ts`
- Test: `src/hooks/useTodos.test.tsx`

- [ ] **Step 1: `useTodos` の失敗するテストを書く**

```tsx
import { act, renderHook } from "@testing-library/react";
import { useTodos, TODO_STORAGE_KEY } from "./useTodos";

describe("useTodos", () => {
  beforeEach(() => {
    window.localStorage.clear();
  });

  it("trim 済みタイトルで TODO を追加する", () => {
    const { result } = renderHook(() => useTodos());

    act(() => {
      result.current.addTodo("  Buy milk  ");
    });

    expect(result.current.todos).toHaveLength(1);
    expect(result.current.todos[0]).toMatchObject({
      title: "Buy milk",
      completed: false
    });
  });

  it("空文字は追加しない", () => {
    const { result } = renderHook(() => useTodos());

    act(() => {
      result.current.addTodo("   ");
    });

    expect(result.current.todos).toEqual([]);
  });

  it("完了切替と削除ができる", () => {
    const { result } = renderHook(() => useTodos());

    act(() => {
      result.current.addTodo("Buy milk");
    });

    const id = result.current.todos[0].id;

    act(() => {
      result.current.toggleTodo(id);
    });

    expect(result.current.todos[0].completed).toBe(true);

    act(() => {
      result.current.removeTodo(id);
    });

    expect(result.current.todos).toEqual([]);
  });

  it("保存済み TODO を復元する", () => {
    window.localStorage.setItem(
      TODO_STORAGE_KEY,
      JSON.stringify([{ id: "seed-1", title: "Read book", completed: true }])
    );

    const { result } = renderHook(() => useTodos());

    expect(result.current.todos).toEqual([
      { id: "seed-1", title: "Read book", completed: true }
    ]);
  });
});
```

- [ ] **Step 2: テストを実行して失敗を確認する**

Run:

```bash
npm run test -- src/hooks/useTodos.test.tsx
```

Expected: `Cannot find module './useTodos'` か同等の失敗になる

- [ ] **Step 3: 最小実装を書く**

```ts
import { useEffect, useState } from "react";
import type { Todo } from "../types/todo";

export const TODO_STORAGE_KEY = "todo-app-items";

function readStoredTodos(): Todo[] {
  try {
    const raw = window.localStorage.getItem(TODO_STORAGE_KEY);
    return raw ? (JSON.parse(raw) as Todo[]) : [];
  } catch (error) {
    console.error("Failed to read todos from localStorage", error);
    return [];
  }
}

export function useTodos() {
  const [todos, setTodos] = useState<Todo[]>(() => readStoredTodos());

  useEffect(() => {
    try {
      window.localStorage.setItem(TODO_STORAGE_KEY, JSON.stringify(todos));
    } catch (error) {
      console.error("Failed to save todos to localStorage", error);
    }
  }, [todos]);

  const addTodo = (title: string) => {
    const trimmedTitle = title.trim();
    if (!trimmedTitle) return;

    setTodos((currentTodos) => [
      ...currentTodos,
      {
        id: crypto.randomUUID(),
        title: trimmedTitle,
        completed: false
      }
    ]);
  };

  const toggleTodo = (id: string) => {
    setTodos((currentTodos) =>
      currentTodos.map((todo) =>
        todo.id === id ? { ...todo, completed: !todo.completed } : todo
      )
    );
  };

  const removeTodo = (id: string) => {
    setTodos((currentTodos) =>
      currentTodos.filter((todo) => todo.id !== id)
    );
  };

  return { todos, addTodo, toggleTodo, removeTodo };
}
```

- [ ] **Step 4: テストを再実行して成功を確認する**

Run:

```bash
npm run test -- src/hooks/useTodos.test.tsx
```

Expected: `4 passed`

### Task 3: `TodoForm` を TDD で作る

**Files:**
- Create: `src/components/TodoForm.tsx`
- Test: `src/components/TodoForm.test.tsx`

- [ ] **Step 1: 失敗するフォームテストを書く**

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { TodoForm } from "./TodoForm";

it("trim 済みタイトルを submit して入力欄を空に戻す", async () => {
  const user = userEvent.setup();
  const onAddTodo = vi.fn();

  render(<TodoForm onAddTodo={onAddTodo} />);

  await user.type(screen.getByLabelText("TODO"), "  Buy milk  ");
  await user.click(screen.getByRole("button", { name: "Add" }));

  expect(onAddTodo).toHaveBeenCalledWith("Buy milk");
  expect(screen.getByLabelText("TODO")).toHaveValue("");
});
```

- [ ] **Step 2: テストを実行して失敗を確認する**

Run:

```bash
npm run test -- src/components/TodoForm.test.tsx
```

Expected: `Cannot find module './TodoForm'` か同等の失敗になる

- [ ] **Step 3: 最小実装を書く**

```tsx
import { FormEvent, useState } from "react";

type TodoFormProps = {
  onAddTodo: (title: string) => void;
};

export function TodoForm({ onAddTodo }: TodoFormProps) {
  const [title, setTitle] = useState("");

  const handleSubmit = (event: FormEvent<HTMLFormElement>) => {
    event.preventDefault();

    const trimmedTitle = title.trim();
    if (!trimmedTitle) return;

    onAddTodo(trimmedTitle);
    setTitle("");
  };

  return (
    <form className="todo-form" onSubmit={handleSubmit}>
      <label className="sr-only" htmlFor="todo-title">
        TODO
      </label>
      <input
        id="todo-title"
        value={title}
        onChange={(event) => setTitle(event.target.value)}
        placeholder="Add a task"
      />
      <button type="submit">Add</button>
    </form>
  );
}
```

- [ ] **Step 4: テストを再実行して成功を確認する**

Run:

```bash
npm run test -- src/components/TodoForm.test.tsx
```

Expected: `1 passed`

### Task 4: `TodoList` と `TodoItem` を TDD で作る

**Files:**
- Create: `src/components/TodoList.tsx`
- Create: `src/components/TodoItem.tsx`
- Test: `src/components/TodoList.test.tsx`

- [ ] **Step 1: 一覧表示の失敗するテストを書く**

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { TodoList } from "./TodoList";

it("TODO が空のとき空状態を表示する", () => {
  render(
    <TodoList todos={[]} onToggleTodo={vi.fn()} onRemoveTodo={vi.fn()} />
  );

  expect(screen.getByText("No tasks yet. Add your first one.")).toBeInTheDocument();
});

it("TODO 一覧を表示し、操作イベントを親へ渡す", async () => {
  const user = userEvent.setup();
  const onToggleTodo = vi.fn();
  const onRemoveTodo = vi.fn();

  render(
    <TodoList
      todos={[{ id: "1", title: "Buy milk", completed: false }]}
      onToggleTodo={onToggleTodo}
      onRemoveTodo={onRemoveTodo}
    />
  );

  await user.click(screen.getByRole("checkbox", { name: "Buy milk" }));
  await user.click(screen.getByRole("button", { name: "Delete Buy milk" }));

  expect(onToggleTodo).toHaveBeenCalledWith("1");
  expect(onRemoveTodo).toHaveBeenCalledWith("1");
});
```

- [ ] **Step 2: テストを実行して失敗を確認する**

Run:

```bash
npm run test -- src/components/TodoList.test.tsx
```

Expected: `Cannot find module './TodoList'` か同等の失敗になる

- [ ] **Step 3: 最小実装を書く**

```tsx
// src/components/TodoItem.tsx
import type { Todo } from "../types/todo";

type TodoItemProps = {
  todo: Todo;
  onToggleTodo: (id: string) => void;
  onRemoveTodo: (id: string) => void;
};

export function TodoItem({ todo, onToggleTodo, onRemoveTodo }: TodoItemProps) {
  return (
    <li className="todo-item">
      <label>
        <input
          type="checkbox"
          checked={todo.completed}
          onChange={() => onToggleTodo(todo.id)}
          aria-label={todo.title}
        />
        <span className={todo.completed ? "is-completed" : ""}>{todo.title}</span>
      </label>
      <button type="button" onClick={() => onRemoveTodo(todo.id)}>
        Delete {todo.title}
      </button>
    </li>
  );
}
```

```tsx
// src/components/TodoList.tsx
import type { Todo } from "../types/todo";
import { TodoItem } from "./TodoItem";

type TodoListProps = {
  todos: Todo[];
  onToggleTodo: (id: string) => void;
  onRemoveTodo: (id: string) => void;
};

export function TodoList({
  todos,
  onToggleTodo,
  onRemoveTodo
}: TodoListProps) {
  if (todos.length === 0) {
    return <p className="empty-state">No tasks yet. Add your first one.</p>;
  }

  return (
    <ul className="todo-list">
      {todos.map((todo) => (
        <TodoItem
          key={todo.id}
          todo={todo}
          onToggleTodo={onToggleTodo}
          onRemoveTodo={onRemoveTodo}
        />
      ))}
    </ul>
  );
}
```

- [ ] **Step 4: テストを再実行して成功を確認する**

Run:

```bash
npm run test -- src/components/TodoList.test.tsx
```

Expected: `2 passed`

### Task 5: `App` を構成して Calm Panel UI を実装する

**Files:**
- Create: `src/App.tsx`
- Create: `src/App.css`
- Test: `src/App.test.tsx`

- [ ] **Step 1: 画面全体の失敗する統合テストを書く**

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import App from "./App";

it("追加、完了切替、削除が画面上で行える", async () => {
  const user = userEvent.setup();

  render(<App />);

  await user.type(screen.getByLabelText("TODO"), "Buy milk");
  await user.click(screen.getByRole("button", { name: "Add" }));

  expect(screen.getByRole("checkbox", { name: "Buy milk" })).toBeInTheDocument();

  await user.click(screen.getByRole("checkbox", { name: "Buy milk" }));
  expect(screen.getByText("Buy milk")).toHaveClass("is-completed");

  await user.click(screen.getByRole("button", { name: "Delete Buy milk" }));
  expect(screen.getByText("No tasks yet. Add your first one.")).toBeInTheDocument();
});
```

- [ ] **Step 2: テストを実行して失敗を確認する**

Run:

```bash
npm run test -- src/App.test.tsx
```

Expected: `Cannot find module './App'` か、必要な UI が存在しない失敗になる

- [ ] **Step 3: `App` とスタイルを最小実装する**

```tsx
import "./App.css";
import { TodoForm } from "./components/TodoForm";
import { TodoList } from "./components/TodoList";
import { useTodos } from "./hooks/useTodos";

export default function App() {
  const { todos, addTodo, toggleTodo, removeTodo } = useTodos();

  return (
    <main className="app-shell">
      <section className="todo-card">
        <p className="eyebrow">Today</p>
        <h1>My Tasks</h1>
        <TodoForm onAddTodo={addTodo} />
        <TodoList
          todos={todos}
          onToggleTodo={toggleTodo}
          onRemoveTodo={removeTodo}
        />
      </section>
    </main>
  );
}
```

```css
.app-shell {
  min-height: 100vh;
  display: grid;
  place-items: center;
  padding: 24px;
  background: linear-gradient(180deg, #f6efe8 0%, #fffdf9 100%);
}

.todo-card {
  width: min(100%, 420px);
  padding: 24px;
  border: 1px solid #d8c9ba;
  border-radius: 20px;
  background: rgba(255, 255, 255, 0.88);
  box-shadow: 0 18px 40px rgba(114, 84, 56, 0.08);
}
```

- [ ] **Step 4: 統合テストを再実行して成功を確認する**

Run:

```bash
npm run test -- src/App.test.tsx
```

Expected: `1 passed`

### Task 6: 永続化と見た目の仕上げを確認する

**Files:**
- Modify: `src/App.tsx`
- Modify: `src/App.css`
- Modify: `src/index.css`
- Modify: `src/App.test.tsx`

- [ ] **Step 1: 保存データ復元の失敗する統合テストを追加する**

```tsx
it("localStorage に保存済みの TODO を初期表示で復元する", () => {
  window.localStorage.setItem(
    "todo-app-items",
    JSON.stringify([{ id: "seed-1", title: "Read book", completed: true }])
  );

  render(<App />);

  expect(screen.getByRole("checkbox", { name: "Read book" })).toBeChecked();
});
```

- [ ] **Step 2: テストを実行して失敗有無を確認し、必要なら実装を補正する**

Run:

```bash
npm run test -- src/App.test.tsx
```

Expected: 既に実装が足りていれば PASS、不足があれば `localStorage` 復元に関する FAIL が出る

- [ ] **Step 3: Calm Panel の仕上げとレスポンシブ調整を行う**

```css
.todo-form {
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 12px;
  margin-bottom: 16px;
}

.todo-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  padding: 12px;
  border: 1px solid #eadfd2;
  border-radius: 12px;
  background: #fffaf4;
}

@media (max-width: 480px) {
  .todo-form {
    grid-template-columns: 1fr;
  }
}
```

- [ ] **Step 4: 画面テストを再実行して成功を確認する**

Run:

```bash
npm run test:run
```

Expected: 全テストが PASS する

### Task 7: 手動確認と最終検証を行う

**Files:**
- Modify: なし
- Test: 既存テスト一式

- [ ] **Step 1: 自動テストを通す**

Run:

```bash
npm run test:run
```

Expected: 全テスト PASS

- [ ] **Step 2: 本番ビルドを通す**

Run:

```bash
npm run build
```

Expected: `vite build` が成功し、`dist/` が生成される

- [ ] **Step 3: ブラウザで手動確認する**

Run:

```bash
npm run dev
```

Expected:
- PC 幅で中央カード表示になる
- スマホ幅でもカードとフォームが潰れない
- TODO の追加、完了、削除ができる
- リロード後に保存内容が残る

- [ ] **Step 4: 受け入れ条件をチェックする**

確認項目:
- TODO を追加、完了、削除できる
- `localStorage` 復元が効く
- UI と状態管理の責務が分かれている
- スコープ外の機能を増やしていない
